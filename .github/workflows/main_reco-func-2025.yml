# .github/workflows/main_reco-func-2025.yml
name: CI/CD Azure Function App - reco-func-2025

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  # Fichier ZIP envoyé sur Azure (généré plus bas)
  AZURE_FUNCTIONAPP_PACKAGE_PATH: 'function.zip'
  PYTHON_VERSION: '3.10'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Setup Python ${{ env.PYTHON_VERSION }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      # --------------------------------------------------------------------
      # ⚙️  INSTALL DEPENDENCIES
      # --------------------------------------------------------------------
      # Le requirements.txt est à la racine du dépôt. S'il se trouve plutôt
      # dans azure_func/, changez "requirements.txt" par ce chemin.
      - name: 📦 Install dependencies from requirements.txt
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # --------------------------------------------------------------------
      # 📦 PACKAGE THE FUNCTION APP
      # --------------------------------------------------------------------
      - name: 📚 Package Function App into ZIP
        run: |
          # On se place dans le dossier contenant le code Azure Function
          cd azure_func
          zip -r ../function.zip .

      - name: ⬆️ Upload artifact for deployment
        uses: actions/upload-artifact@v4
        with:
          name: function-package
          path: function.zip

      - name: ⬇️ Download artifact for deployment
        uses: actions/download-artifact@v4
        with:
          name: function-package

      - name: 🚀 Deploy to Azure Functions
        uses: azure/functions-action@v1
        with:
          app-name: 'reco-func-2025'
          package: '${{ env.AZURE_FUNCTIONAPP_PACKAGE_PATH }}'